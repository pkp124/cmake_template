# Requirements for PyAEDT Environment
# Use this with Python 3.8+ for AEDT data extraction

# Core PyAEDT
pyaedt>=0.7.0

# Data processing
numpy>=1.21.0
pandas>=1.3.0
scipy>=1.7.0

# Data handling and conversion
h5py>=3.6.0
scikit-rf>=0.24.0

# Configuration
python-dotenv>=0.19.0
pyyaml>=6.0

# Logging
loguru>=0.6.0
tqdm>=4.62.0

# Visualization (optional for PyAEDT side)
matplotlib>=3.4.0

# Note: This environment is for PyAEDT data extraction only
# ADS automation should run in a separate environment (see requirements_ads.txt)
